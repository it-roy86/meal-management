# 구내식당 식수 관리 웹앱 기획/설계서

> 작성일: 2026-04-07  
> 작성자: 한수 (개발자)  
> Claude와 함께 기획/설계

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | 구내식당 식수 관리 웹앱 |
| 목적 | 종이 명단 → 디지털 자동화 (매일 종이에 기록 → 엑셀 정리 작업 제거) |
| 대상 회사 | 대박유통, (주)복수 |
| 개발 스택 | Spring Boot (백엔드), Vue.js (프론트엔드), PostgreSQL (DB) |
| 현재 단계 | 기획 & 설계 완료 |

---

## 2. 배경 및 문제점

- 어머니가 구내식당 운영 중 매일 두 회사의 식사 명단을 종이에 기록
- 기록한 종이를 사진으로 보내면 아드님이 엑셀로 정리
- 월말마다 회사별 정산 후 이메일로 청구서 발송

**현재 흐름:** 종이 기록 → 사진 전송 → 엑셀 수동 입력 → 월말 정산

**목표 흐름:** 앱 직접 입력 → 자동 정리 → 월말 자동 정산

---

## 3. 사용자 및 권한

| 역할 | 대상 | 권한 |
|------|------|------|
| ADMIN | 아드님 (개발자) | 전체 데이터 관리, 설정, 정산, 사용자 계정 관리 |
| OPERATOR | 어머니 (식당 운영자) | 일일 식사 인원 입력, 당일 기록 수정 |
| VIEWER | 경리담당자 (회사별) | 자기 회사 데이터만 읽기 전용 조회 |

> VIEWER는 자기 회사 데이터만 접근 가능 (대박유통 담당자 → 대박유통 데이터만)

---

## 4. 회사 및 팀 정보

### 대박유통
- 팀: 대박유통 (단일팀)
- 중식 단가: 7,000원
- 석식 단가: 7,000원

### (주)복수
- 팀: 본점_사무직, 지점_사무직, 본점_생산직, 육가공_생산직, 외부인
- 중식 단가: 6,500원
- 석식 단가: 6,500원

---

## 5. 핵심 기능

### 기능 01. 일일 식사 인원 입력 ★ 핵심
- 날짜 선택 시 회사/팀별 행 자동 표시
- 중식/석식 인원 숫자 입력 → 소계 자동계산
- 모바일 친화적 UI (어머니가 스마트폰으로 입력)
- 입력/수정 방식: 같은 날짜+팀 데이터는 업데이트 방식으로 처리

### 기능 02. 현황 조회
- 날짜 범위, 회사별 필터링
- VIEWER 역할은 자기 회사 데이터만 자동 필터 적용

### 기능 03. 월별 정산 ★ 중요
- 월 + 회사 선택
- 팀별 중식/석식 인원 및 금액 자동 집계
- 총 식사인원 합계, 총 금액 합계 표시
- 엑셀 파일 다운로드 기능

### 기능 04. 설정 관리 (ADMIN 전용)
- 회사 추가/수정 (회사명, 사업자번호, 담당자 이메일)
- 팀 추가/수정 (팀명, 중식/석식 단가)
- 사용자 계정 생성 및 역할 부여
- 소프트 딜리트 적용 (삭제 대신 is_active = false 처리)

### 기능 05. 메일 발송 (추후 개발)
- 월별 정산 데이터를 회사 경리담당자에게 이메일 발송
- contact_email 컬럼 활용
- API 구조는 메일 기능 추가에 용이하게 미리 설계

---

## 6. DB 테이블 설계 (PostgreSQL)

> 전 테이블 소프트 딜리트 적용 (is_active)  
> PK는 시퀀스 방식 (bigint)

### 6-1. user (사용자)

| 컬럼명 | 한글명 | 타입 | 키 |
|--------|--------|------|----|
| id | 사용자 고유번호 | bigint | PK |
| username | 로그인 아이디 | varchar | |
| password | 비밀번호 (BCrypt 암호화) | varchar | |
| role | 역할 (ADMIN/OPERATOR/VIEWER) | enum | |
| company_id | 소속 회사 (VIEWER만 사용, nullable) | bigint | FK |
| is_active | 사용 여부 | boolean | |
| created_at | 생성일시 | timestamp | |
| updated_at | 수정일시 | timestamp | |

### 6-2. company (회사)

| 컬럼명 | 한글명 | 타입 | 키 |
|--------|--------|------|----|
| id | 회사 고유번호 | bigint | PK |
| company_name | 회사명 | varchar | |
| business_number | 사업자번호 | varchar(10) | |
| contact_email | 경리담당자 이메일 | varchar | |
| is_active | 사용 여부 | boolean | |
| created_at | 생성일시 | timestamp | |
| updated_at | 수정일시 | timestamp | |

### 6-3. company_team (회사 팀)

| 컬럼명 | 한글명 | 타입 | 키 |
|--------|--------|------|----|
| id | 팀 고유번호 | bigint | PK |
| company_id | 회사 고유번호 | bigint | FK |
| team_name | 팀명 | varchar | |
| lunch_price | 중식 단가 | int | |
| dinner_price | 석식 단가 | int | |
| is_active | 사용 여부 | boolean | |
| created_at | 생성일시 | timestamp | |
| updated_at | 수정일시 | timestamp | |

### 6-4. meal_record (식사 기록)

| 컬럼명 | 한글명 | 타입 | 키 |
|--------|--------|------|----|
| id | 기록 고유번호 | bigint | PK |
| record_date | 식사 일자 | date | |
| company_id | 회사 고유번호 | bigint | FK |
| company_team_id | 팀 고유번호 | bigint | FK |
| lunch_count | 중식 인원 | int | |
| dinner_count | 석식 인원 | int | |
| total_count | 소계 인원 | int | |
| total_amount | 합계 금액 | int | |
| created_by | 입력자 (user 참조) | bigint | FK |
| created_at | 생성일시 | timestamp | |
| updated_at | 수정일시 | timestamp | |

---

## 7. 기술 스택

| 영역 | 기술 |
|------|------|
| 백엔드 API | Spring Boot |
| 프론트엔드 | Vue.js |
| 데이터베이스 | PostgreSQL |
| 인증/권한 | Spring Security + JWT |
| 배포 (추후) | Vercel / Railway |

---

## 8. 개발 단계

| 단계 | 내용 | 상태 |
|------|------|------|
| Step 0 | 기획 & DB 설계 확정 | ✅ 완료 |
| Step 1 | Spring Boot 프로젝트 셋업 & API 목록 설계 | 예정 |
| Step 2 | Spring Security + JWT 인증/권한 구현 | 예정 |
| Step 3 | Vue.js 화면 개발 (입력 → 조회 → 정산 순) | 예정 |
| Step 4 | 테스트 & 배포 | 예정 |
| Step 5 | 메일링 시스템 개발 | 추후 |

---

## 9. 설계 주요 결정사항

- **소프트 딜리트 적용**: 팀/회사 삭제 시 DELETE 대신 `is_active = false` 처리 → 과거 정산 데이터 보호
- **단가 스냅샷 저장**: `meal_record.total_amount`에 입력 시점 단가 기반 금액 저장 → 나중에 단가 변경되어도 과거 정산 영향 없음
- **입력자 추적**: `created_by`로 누가 입력했는지 추적 가능
- **VIEWER 데이터 격리**: `user.company_id`로 경리담당자별 접근 회사 제한
- **중복 방지 전략**: UNIQUE 제약 대신 입력 화면에서 날짜+팀 기준으로 기존 데이터 불러와 수정하는 방식 채택

---

## 10. 트러블슈팅 & 운영 노트 (2026-09-09)

로컬 개발/운영 중 발생한 이슈와 해결 방법을 기록해요. 비슷한 문제가 재발하면 여기부터 확인해요.

### 10-1. `meal_record_pkey` 중복 키 오류
- **증상**: 식사 기록 저장 시 `중복된 키 값이 "meal_record_pkey" 고유 제약 조건을 위반함` 오류 발생
- **원인**: PostgreSQL identity 시퀀스가 실제 테이블의 최대 id보다 뒤처짐 (수동 데이터 조작, DB 복원 등으로 발생)
- **해결**: 시퀀스를 실제 최대값 이후로 재동기화
  ```sql
  SELECT setval(
      pg_get_serial_sequence('meal_record', 'id'),
      COALESCE((SELECT MAX(id) FROM meal_record), 0) + 1,
      false
  );
  ```

### 10-2. admin 계정 비밀번호 로그인 불가
- **증상**: 설정해둔 비밀번호로 로그인이 안 됨
- **원인**: 이 프로젝트엔 비밀번호 변경 API가 없어서, admin 계정 비밀번호는 최초 생성 시 `DataInitializer`가 `ADMIN_PASSWORD` 환경변수(기본값 `1234`)로 암호화해 저장하는 게 유일한 경로임. DB 초기화/계정 재생성 등으로 원래 의도한 비밀번호와 다른 값이 저장됨
- **해결**: BCrypt로 새 해시를 생성해 DB에 직접 UPDATE
  ```sql
  UPDATE "user" SET password = '<BCrypt 해시>' WHERE username = 'admin';
  ```
- **참고**: `.env`는 `docker-compose`에서만 읽는 파일이라 로컬에서 `mvnw`로 직접 실행할 땐 적용되지 않음

### 10-3. 로그인 시 403 Forbidden (CORS)
- **증상**: 프론트엔드에서 로그인 요청 시 403 Forbidden
- **원인**: Vite 개발 서버 포트가 5173이 아닌 5174로 떠서 (5173을 이전 프로세스가 점유), `SecurityConfig`의 CORS 허용 origin 목록(`5173`만 등록)과 불일치 → Spring Security가 요청 자체를 403으로 거부
- **해결**:
  1. `SecurityConfig.corsConfigurationSource()`에 `5174`, `5175` 포트 추가
  2. 5173 포트를 점유하던 좀비 프로세스 종료 (`netstat -ano | findstr :5173` → `taskkill /PID <pid> /F`)

### 10-4. 파일 로그 & 로그 레벨 설정
- **필요성**: 회사에서 쓰던 것처럼 콘솔뿐 아니라 파일로도 로그를 남기고 싶음
- **해결**: `logback-spring.xml` 추가
  - `logs/meal-management.log` (전체), `logs/meal-management-error.log` (ERROR만) 분리 저장
  - 일자별/10MB 기준 자동 로테이션, 30일 보관, 총 1GB 초과 시 자동 삭제
  - `local` 프로파일 활성화 시(`-Dspring.profiles.active=local`) 로그 레벨을 `DEBUG`까지 확인 가능, 기본(운영)은 `INFO`
  - 기존 `DataInitializer`의 `System.out.println`은 파일에 안 남기 때문에 `@Slf4j` 로거로 교체
