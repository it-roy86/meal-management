# 구내식당 식수 관리 웹앱 기획/설계서

> 작성일: 2026-04-07  
> 작성자: 한수 (개발자)  
> Claude와 함께 기획/설계

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | 구내식당 식수 관리 웹앱 |
| 목적 | 종이 명단 → 디지털 자동화 (매일 종이에 기록 → 엑셀 정리 작업 제거) |
| 대상 회사 | 대박유통, (주)복수 |
| 개발 스택 | Spring Boot (백엔드), Vue.js (프론트엔드), PostgreSQL (DB) |
| 현재 단계 | 기획 & 설계 완료 |

---

## 2. 배경 및 문제점

- 어머니가 구내식당 운영 중 매일 두 회사의 식사 명단을 종이에 기록
- 기록한 종이를 사진으로 보내면 아드님이 엑셀로 정리
- 월말마다 회사별 정산 후 이메일로 청구서 발송

**현재 흐름:** 종이 기록 → 사진 전송 → 엑셀 수동 입력 → 월말 정산

**목표 흐름:** 앱 직접 입력 → 자동 정리 → 월말 자동 정산

---

## 3. 사용자 및 권한

| 역할 | 대상 | 권한 |
|------|------|------|
| ADMIN | 아드님 (개발자) | 전체 데이터 관리, 설정, 정산, 사용자 계정 관리 |
| OPERATOR | 어머니 (식당 운영자) | 일일 식사 인원 입력, 당일 기록 수정 |
| VIEWER | 경리담당자 (회사별) | 자기 회사 데이터만 읽기 전용 조회 |

> VIEWER는 자기 회사 데이터만 접근 가능 (대박유통 담당자 → 대박유통 데이터만)

---

## 4. 회사 및 팀 정보

### 대박유통
- 팀: 대박유통 (단일팀)
- 중식 단가: 7,000원
- 석식 단가: 7,000원

### (주)복수
- 팀: 본점_사무직, 지점_사무직, 본점_생산직, 육가공_생산직, 외부인
- 중식 단가: 6,500원
- 석식 단가: 6,500원

---

## 5. 핵심 기능

### 기능 01. 일일 식사 인원 입력 ★ 핵심
- 날짜 선택 시 회사/팀별 행 자동 표시
- 중식/석식 인원 숫자 입력 → 소계 자동계산
- 모바일 친화적 UI (어머니가 스마트폰으로 입력)
- 입력/수정 방식: 같은 날짜+팀 데이터는 업데이트 방식으로 처리

### 기능 02. 현황 조회
- 날짜 범위, 회사별 필터링
- VIEWER 역할은 자기 회사 데이터만 자동 필터 적용

### 기능 03. 월별 정산 ★ 중요
- 월 + 회사 선택
- 팀별 중식/석식 인원 및 금액 자동 집계
- 총 식사인원 합계, 총 금액 합계 표시
- 엑셀 파일 다운로드 기능

### 기능 04. 설정 관리 (ADMIN 전용)
- 회사 추가/수정 (회사명, 사업자번호, 담당자 이메일)
- 팀 추가/수정 (팀명, 중식/석식 단가)
- 사용자 계정 생성 및 역할 부여
- 소프트 딜리트 적용 (삭제 대신 is_active = false 처리)

### 기능 05. 메일 발송 (추후 개발)
- 월별 정산 데이터를 회사 경리담당자에게 이메일 발송
- contact_email 컬럼 활용
- API 구조는 메일 기능 추가에 용이하게 미리 설계

---

## 6. DB 테이블 설계 (PostgreSQL)

> 전 테이블 소프트 딜리트 적용 (is_active)  
> PK는 시퀀스 방식 (bigint)

### 6-1. user (사용자)

| 컬럼명 | 한글명 | 타입 | 키 |
|--------|--------|------|----|
| id | 사용자 고유번호 | bigint | PK |
| username | 로그인 아이디 | varchar | |
| password | 비밀번호 (BCrypt 암호화) | varchar | |
| role | 역할 (ADMIN/OPERATOR/VIEWER) | enum | |
| company_id | 소속 회사 (VIEWER만 사용, nullable) | bigint | FK |
| is_active | 사용 여부 | boolean | |
| created_at | 생성일시 | timestamp | |
| updated_at | 수정일시 | timestamp | |

### 6-2. company (회사)

| 컬럼명 | 한글명 | 타입 | 키 |
|--------|--------|------|----|
| id | 회사 고유번호 | bigint | PK |
| company_name | 회사명 | varchar | |
| business_number | 사업자번호 | varchar(10) | |
| contact_email | 경리담당자 이메일 | varchar | |
| is_active | 사용 여부 | boolean | |
| created_at | 생성일시 | timestamp | |
| updated_at | 수정일시 | timestamp | |

### 6-3. company_team (회사 팀)

| 컬럼명 | 한글명 | 타입 | 키 |
|--------|--------|------|----|
| id | 팀 고유번호 | bigint | PK |
| company_id | 회사 고유번호 | bigint | FK |
| team_name | 팀명 | varchar | |
| lunch_price | 중식 단가 | int | |
| dinner_price | 석식 단가 | int | |
| is_active | 사용 여부 | boolean | |
| created_at | 생성일시 | timestamp | |
| updated_at | 수정일시 | timestamp | |

### 6-4. meal_record (식사 기록)

| 컬럼명 | 한글명 | 타입 | 키 |
|--------|--------|------|----|
| id | 기록 고유번호 | bigint | PK |
| record_date | 식사 일자 | date | |
| company_id | 회사 고유번호 | bigint | FK |
| company_team_id | 팀 고유번호 | bigint | FK |
| lunch_count | 중식 인원 | int | |
| dinner_count | 석식 인원 | int | |
| total_count | 소계 인원 | int | |
| total_amount | 합계 금액 | int | |
| created_by | 입력자 (user 참조) | bigint | FK |
| created_at | 생성일시 | timestamp | |
| updated_at | 수정일시 | timestamp | |

---

## 7. 기술 스택

| 영역 | 기술 |
|------|------|
| 백엔드 API | Spring Boot |
| 프론트엔드 | Vue.js |
| 데이터베이스 | PostgreSQL |
| 인증/권한 | Spring Security + JWT |
| 배포 (추후) | Vercel / Railway |

---

## 8. 개발 단계

| 단계 | 내용 | 상태 |
|------|------|------|
| Step 0 | 기획 & DB 설계 확정 | ✅ 완료 |
| Step 1 | Spring Boot 프로젝트 셋업 & API 목록 설계 | 예정 |
| Step 2 | Spring Security + JWT 인증/권한 구현 | 예정 |
| Step 3 | Vue.js 화면 개발 (입력 → 조회 → 정산 순) | 예정 |
| Step 4 | 테스트 & 배포 | 예정 |
| Step 5 | 메일링 시스템 개발 | 추후 |

---

## 9. 설계 주요 결정사항

- **소프트 딜리트 적용**: 팀/회사 삭제 시 DELETE 대신 `is_active = false` 처리 → 과거 정산 데이터 보호
- **단가 스냅샷 저장**: `meal_record.total_amount`에 입력 시점 단가 기반 금액 저장 → 나중에 단가 변경되어도 과거 정산 영향 없음
- **입력자 추적**: `created_by`로 누가 입력했는지 추적 가능
- **VIEWER 데이터 격리**: `user.company_id`로 경리담당자별 접근 회사 제한
- **중복 방지 전략**: UNIQUE 제약 대신 입력 화면에서 날짜+팀 기준으로 기존 데이터 불러와 수정하는 방식 채택

---

## 10. 트러블슈팅 & 운영 노트 (2026-09-09)

로컬 개발/운영 중 발생한 이슈와 해결 방법을 기록해요. 비슷한 문제가 재발하면 여기부터 확인해요.

### 10-1. `meal_record_pkey` 중복 키 오류
- **증상**: 식사 기록 저장 시 `중복된 키 값이 "meal_record_pkey" 고유 제약 조건을 위반함` 오류 발생
- **원인**: PostgreSQL identity 시퀀스가 실제 테이블의 최대 id보다 뒤처짐 (수동 데이터 조작, DB 복원 등으로 발생)
- **해결**: 시퀀스를 실제 최대값 이후로 재동기화
  ```sql
  SELECT setval(
      pg_get_serial_sequence('meal_record', 'id'),
      COALESCE((SELECT MAX(id) FROM meal_record), 0) + 1,
      false
  );
  ```

### 10-2. admin 계정 비밀번호 로그인 불가
- **증상**: 설정해둔 비밀번호로 로그인이 안 됨
- **원인**: 이 프로젝트엔 비밀번호 변경 API가 없어서, admin 계정 비밀번호는 최초 생성 시 `DataInitializer`가 `ADMIN_PASSWORD` 환경변수(기본값 `1234`)로 암호화해 저장하는 게 유일한 경로임. DB 초기화/계정 재생성 등으로 원래 의도한 비밀번호와 다른 값이 저장됨
- **해결**: BCrypt로 새 해시를 생성해 DB에 직접 UPDATE
  ```sql
  UPDATE "user" SET password = '<BCrypt 해시>' WHERE username = 'admin';
  ```
- **참고**: `.env`는 `docker-compose`에서만 읽는 파일이라 로컬에서 `mvnw`로 직접 실행할 땐 적용되지 않음

### 10-3. 로그인 시 403 Forbidden (CORS)
- **증상**: 프론트엔드에서 로그인 요청 시 403 Forbidden
- **원인**: Vite 개발 서버 포트가 5173이 아닌 5174로 떠서 (5173을 이전 프로세스가 점유), `SecurityConfig`의 CORS 허용 origin 목록(`5173`만 등록)과 불일치 → Spring Security가 요청 자체를 403으로 거부
- **해결**:
  1. `SecurityConfig.corsConfigurationSource()`에 `5174`, `5175` 포트 추가
  2. 5173 포트를 점유하던 좀비 프로세스 종료 (`netstat -ano | findstr :5173` → `taskkill /PID <pid> /F`)

### 10-4. 파일 로그 & 로그 레벨 설정
- **필요성**: 회사에서 쓰던 것처럼 콘솔뿐 아니라 파일로도 로그를 남기고 싶음
- **해결**: `logback-spring.xml` 추가
  - `logs/meal-management.log` (전체), `logs/meal-management-error.log` (ERROR만) 분리 저장
  - 일자별/10MB 기준 자동 로테이션, 30일 보관, 총 1GB 초과 시 자동 삭제
  - `local` 프로파일 활성화 시(`-Dspring.profiles.active=local`) 로그 레벨을 `DEBUG`까지 확인 가능, 기본(운영)은 `INFO`
  - 기존 `DataInitializer`의 `System.out.println`은 파일에 안 남기 때문에 `@Slf4j` 로거로 교체

---

## 11. 보안 점검 & 인프라 변경 이력 (2026-09-12)

### 11-1. JWT 시크릿 키 하드코딩 → 환경변수 분리
- **발견 경위**: 보안 관점에서 인증 구조를 점검하던 중 `JwtUtil.java`에 JWT 서명 시크릿 키가 코드에 평문으로 하드코딩되어 git에 커밋된 것을 발견함
- **문제점**: 저장소 접근 권한이 있는 사람은 누구나 이 키로 임의의 role(예: ADMIN)을 가진 토큰을 직접 위조할 수 있는 구조였음
- **조치**:
  - `JwtUtil`의 `SECRET_KEY`를 `@Value("${JWT_SECRET:...}")`로 변경해 환경변수 주입 방식으로 전환 (환경변수 미설정 시 로컬 개발용 기본값 사용)
  - `docker-compose.yml`의 backend 서비스에 `JWT_SECRET` 환경변수 전달 추가
  - 로컬 `.env`(git 미포함)에 새 랜덤 시크릿 값 추가
- **참고**: 운영 서버에도 `JWT_SECRET` 환경변수를 반드시 별도로 설정해야 함. 값을 처음 설정/변경하는 순간 기존에 발급된 모든 JWT 토큰이 무효화되어 로그인 중이던 사용자 전원이 강제 로그아웃됨 → 사용자가 적은 시간대에 적용 권장
- 관련 커밋: `fix: JWT 시크릿 키를 환경변수로 분리`

### 11-2. AWS Lightsail 사용 중지 및 홈서버 이전 계획
- **배경**: 개발자(아드님) 이사 예정. 이사 후에는 AWS Lightsail 대신 자택에 홈서버를 구축해서 직접 배포/운영할 계획 (2026-09-12 기준 아직 미착수, Lightsail은 이미 사용 중지함)
- **주의**: 7장 기술 스택의 배포 항목은 최초 기획 당시 값(Vercel / Railway)이라 이미 실제(Lightsail)와 다르고, 이제 Lightsail도 곧 홈서버로 바뀔 예정이라 계속 변경 중임. 홈서버 구축이 완료되면 이 문서 7장과 README.md 인프라 정보를 최신 상태로 갱신할 것
- **홈서버 전환 시 체크리스트**: 포트포워딩 최소화(DB 등 내부 포트 외부 노출 금지), 리버스 프록시 + HTTPS(Let's Encrypt), 고정 IP 아니면 DDNS 또는 Cloudflare Tunnel/Tailscale 사용 검토, 정전/재부팅 후 자동 기동, DB 백업(원격지 포함)

---

## 12. 보안 강화 - 프론트엔드 인증 방식 개선 (2026-09-13)

11장에서 시작한 보안 점검을 이어서, 프론트엔드(`meal-management-front`) 쪽 인증 취약점 2가지를 개선함.

### 12-1. 라우터 네비게이션 가드 추가
- **문제점**: `src/router/index.js`에 접근 제어가 전혀 없어서, role별 메뉴 숨김은 각 화면 컴포넌트의 `v-if`로만 이루어졌음. URL을 직접 입력하면 권한 없는 화면 컴포넌트 자체는 렌더링될 수 있었음 (실제 데이터는 백엔드가 막아서 치명적이진 않았지만 방어 계층이 하나뿐이었음)
- **조치**: 각 라우트에 `meta.roles`(허용 role 목록)를 선언하고 `router.beforeEach`로 검증하도록 추가. 로그인 안 됐으면 로그인 화면으로, role이 안 맞으면 자기 role의 기본 화면으로 되돌림
- **한계**: 어디까지나 UX 방어선. 실제 데이터 접근 권한은 여전히 백엔드(Spring Security)가 최종 검증함

### 12-2. JWT 저장 방식을 localStorage → httpOnly 쿠키로 전환
- **문제점**: JWT를 `localStorage.token`에 저장하고 있어서, XSS 공격이 하나라도 성공하면 토큰을 그대로 탈취당할 수 있었음 (`localStorage`는 JS로 완전히 접근 가능)
- **조치**:
  - 백엔드(`AuthController`): 로그인 성공 시 JWT를 응답 본문이 아니라 `Set-Cookie` 헤더로 내려주도록 변경 (`httpOnly`, `sameSite=Lax`, `path=/`, 만료 24시간). 응답 본문에는 `role`/`username`만 남기고 토큰 필드는 비움
  - 백엔드(`JwtAuthenticationFilter`): 토큰을 `Authorization: Bearer` 헤더뿐 아니라 `token` 쿠키에서도 읽도록 확장 (Postman/`test.http` 등 헤더 기반 수동 테스트는 그대로 동작)
  - 백엔드: `POST /api/auth/logout` 신설 — 만료된 쿠키를 다시 내려줘서 브라우저가 쿠키를 지우게 함 (httpOnly라 프론트 JS로는 직접 못 지움)
  - 프론트(`src/api/axios.js`): `withCredentials: true` 추가, 기존에 localStorage 토큰을 읽어 `Authorization` 헤더에 붙이던 인터셉터 제거
  - 프론트: `role`/`username`만 localStorage에 남기고(화면 분기·표시용), 로그인/로그아웃/라우터 가드 로직을 모두 이 값 기준으로 수정
- **CSRF 관련 트레이드오프**: 쿠키 기반으로 바뀌면서 이론적으로 CSRF 위험이 생기지만, 별도의 CSRF 토큰 방식 대신 쿠키의 `sameSite=Lax` 속성으로 완화하는 선택을 함 (내부 소규모 앱이라 우선 이 정도로 충분하다고 판단; 필요성이 커지면 `CookieCsrfTokenRepository` 등으로 보강 가능)
- **참고**: 쿠키의 `Secure` 속성은 `COOKIE_SECURE` 환경변수로 제어함 (기본 `false` — 아직 HTTPS 미적용이라). 홈서버 이전 후 HTTPS를 적용하면 반드시 `true`로 바꿔야 함
- **검증**: `docker compose up -d --build`로 db/backend/frontend를 띄워서 확인함
  - `POST /api/auth/login` → 응답 본문 `token: null` + `Set-Cookie: token=...; HttpOnly; SameSite=Lax` 확인
  - 쿠키 포함 `GET /api/companies` → 200, 쿠키 없이 요청 → 403 확인
  - `POST /api/auth/logout` → `Set-Cookie: token=; Max-Age=0`로 즉시 만료, 이후 그 쿠키로 재요청 → 403 확인
  - nginx(80포트) 리버스 프록시를 거친 요청에서도 동일하게 동작 확인 (실제 배포 구성과 동일한 경로)
  - 브라우저 UI(로그인 화면 클릭 등)로 직접 눌러보는 테스트는 사용자가 진행

### 12-3. CSRF 토큰 적용 (12-2의 트레이드오프 보강)
- **배경**: 12-2에서 JWT를 쿠키로 바꾸면서 원칙적으로 CSRF 위험이 생겼는데, 당시엔 `sameSite=Lax`만으로 완화하고 별도 CSRF 토큰은 보류했었음. 이번에 마저 적용함
- **조치**:
  - 백엔드(`SecurityConfig`): `CookieCsrfTokenRepository.withHttpOnlyFalse()`로 CSRF 활성화 — 토큰을 `XSRF-TOKEN` 쿠키(JS로 읽기 가능)로 내려주고, `X-XSRF-TOKEN` 요청 헤더로 검증. 기본 `CsrfTokenRequestAttributeHandler`(Xor 인코딩, 서버 렌더링 폼 전용)는 SPA에 안 맞아서 원문 토큰을 그대로 비교하는 `CsrfTokenRequestAttributeHandler`로 교체
  - 백엔드(`CsrfCookieFilter`, 신규): CSRF 토큰은 기본적으로 "누가 실제로 읽을 때"만 지연 계산되어 쿠키가 안 내려가는 경우가 있어서, 매 요청마다 강제로 한 번 읽어 쿠키가 항상 발급되도록 하는 필터 추가 (Spring Security 공식 문서의 SPA 설정 예시와 동일한 방식)
  - 프론트(`src/api/axios.js`): `withXSRFToken: true` 추가 — axios가 쿠키/헤더 이름을 기본값으로 자동 처리해줘서 그 외 코드 변경은 없음
- **왜 쿠키 기반 CSRF 토큰인가**: `XSRF-TOKEN`/`X-XSRF-TOKEN`은 axios의 기본 쿠키명/헤더명과 정확히 일치해서 프론트엔드가 별도 코드를 안 짜도 자동으로 맞물림
- **수동 테스트 시 주의**: `/api/auth/**`처럼 인증이 필요 없는(permitAll) 엔드포인트도 CSRF 검사는 그대로 받음. Postman/`test.http`로 로그인 등 POST를 테스트하려면, 먼저 아무 GET 요청으로 `XSRF-TOKEN` 쿠키를 받아온 뒤 그 값을 `X-XSRF-TOKEN` 헤더에 실어서 보내야 함 (같은 쿠키 세션 유지 필요)
- **검증**: 로컬 docker compose 환경에서 확인함
  - GET 요청 → `Set-Cookie: XSRF-TOKEN=...` 발급 확인
  - CSRF 헤더 없이 로그인 POST → 403 차단 확인
  - 쿠키값을 `X-XSRF-TOKEN` 헤더로 실어서 재요청 → 200 로그인 성공 확인
  - nginx(80포트) 리버스 프록시 경유 시에도 동일하게 동작 확인

---

## 13. 사고 - 로컬 Docker 테스트 중 실제 데이터 볼륨 분리 사고 (2026-09-13)

12장 검증을 위해 로컬에서 `docker compose up`을 하는 과정에서 실제 운영 데이터가 잠시 "분리"됐던 사고. 데이터 손실은 없었고 복구 완료함.

### 13-1. 무슨 일이 있었나
- `docker compose up -d --build` 실행 시 `Container name "/meal-db" is already in use` 충돌 발생 (예전부터 존재하던, Exited 상태의 `meal-db` 컨테이너 때문)
- 문제 해결을 위해 `docker rm meal-db`로 그 컨테이너만 지움 → 이 컨테이너가 실제로는 **익명 볼륨**(`dd061792...`, 2026-09-06 생성)을 쓰고 있었는데, `docker compose up`이 재생성한 새 컨테이너는 `docker-compose.yml`에 정의된 **이름 있는 볼륨**(`meal-management_postgres_data`)을 새로 만들어서 붙임
- 결과적으로 컨테이너는 새로 생겼지만 **실제 데이터(식사 기록 329건, 회사 2개, 2026-05-04~09-04)가 든 볼륨과 연결이 끊긴 빈 DB**로 백엔드가 기동됨 (Hibernate가 테이블을 처음부터 새로 생성한 로그로 뒤늦게 알아챔)

### 13-2. 원인
- `docker rm`은 컨테이너만 지우고 볼륨(특히 익명 볼륨)은 지우지 않고 그대로 둠 → 볼륨이 "고아" 상태로 남음
- 예전 `meal-db` 컨테이너가 애초에 `docker-compose.yml`이 관리하는 이름 있는 볼륨이 아니라 **익명 볼륨**을 쓰고 있었던 것으로 보임 (과거에 `docker run`으로 직접 띄웠거나, 당시 `docker-compose.yml`의 볼륨 설정이 지금과 달랐을 가능성)

### 13-3. 복구 조치
1. `docker volume ls`로 실제 데이터가 든 익명 볼륨(`dd061792...`) 확인
2. 임시 컨테이너로 그 볼륨을 마운트해서 데이터 확인 (`meal_record` 329건, `company` 2건 — 실제 데이터 맞음을 확인). 확인 후 임시 컨테이너만 제거, 볼륨은 그대로 둠
3. `docker run`으로 두 볼륨을 동시에 마운트한 뒤 `cp -a`로 익명 볼륨의 데이터를 이름 있는 볼륨(`meal-management_postgres_data`)에 통째로 복사 (원본 익명 볼륨은 안전을 위해 삭제하지 않고 백업으로 남김)
4. 복사해온 DB의 `postgres` 계정 비밀번호가 `.env`의 `DB_PASSWORD`와 달라서 백엔드 접속 실패(`FATAL: password authentication failed`) 발생 → 컨테이너 내부에서 로컬 소켓으로 접속(trust 인증)해 `ALTER USER postgres WITH PASSWORD '...'`로 비밀번호를 `.env` 값에 맞춰 재설정
5. 백엔드 재시작 후 정상 연결 확인, API로 실제 회사 데이터/로그인 재확인, `meal_record` 329건 그대로 있음을 최종 확인

### 13-4. 재발 방지
- **컨테이너 이름 충돌 같은 문제는 `docker rm <container>`로 개별 삭제하지 말고 `docker compose down`으로 정리할 것** (프로젝트가 관리하는 컨테이너/네트워크를 일관되게 정리해줌)
- **`docker compose down -v`, `docker volume rm`, `docker volume prune`처럼 볼륨을 지우는 명령은 실행 전에 반드시 그 볼륨에 실제 데이터가 있는지 확인하고, 사용자에게 먼저 확인받을 것** (자세한 규칙은 `CLAUDE.md` 참고)
- 로컬 개발/테스트용 DB와 실제 데이터가 섞여 있는 상태 자체가 위험 요소이므로, 가능하면 로컬 테스트는 별도 볼륨/DB로 분리하는 것을 고려해볼 것 (홈서버 이전 시 함께 정리)

### 13-5. (부수 발견) 회사명/팀명 한글 데이터 손상 발견 및 수정
- 복구된 실제 데이터를 웹 화면으로 확인하는 과정에서 회사명/팀명이 전부 `?`로 표시되는 걸 발견함
- DB에서 직접 바이트 확인(`encode(company_name::bytea,'hex')`) 결과, 표시 문제가 아니라 **실제로 리터럴 `?`(0x3F) 문자가 저장되어 있는 진짜 데이터 손상**이었음 (`company.company_name`, `company_team.team_name` 두 컬럼만 영향받음)
- 원인 추정: 오늘 한 볼륨 복사(`cp -a`, 순수 파일 복사라 텍스트 변환 없음)는 원인이 아니고, 10-1에서 언급된 과거 DB 덤프/복원 작업 중 인코딩이 안 맞는 환경(예: Windows 콘솔)에서 한글이 깨진 채로 복원된 것으로 보임. 그 시점에 원본 한글은 이미 덮어써져서 이 DB 안에서는 복구 불가능했음
- **다행히 `meal_record`(식사 기록 329건)는 숫자 ID로 회사/팀을 참조해서 전혀 영향 없었음** — 이름만 바로잡으면 과거 데이터가 그대로 살아남
- **해결**: 사용자가 설정 관리 화면(ADMIN)에서 직접 회사명/팀명을 재입력해서 수정함 → `(주)복수`(팀: 본,지점 사무직 / 본점 생산직 / 육가공 생산직 / 외부인), `(주)대박유통`(팀: 대박유통, 단일팀)으로 정상화 확인
- 이 과정에서 **앱을 통한 정상 입력은 한글이 깨지지 않고 잘 저장되는 것도 확인됨** — 즉 애플리케이션 자체의 인코딩 처리에는 문제가 없고, 과거 일회성 복구 작업에서만 발생한 손상이었음
